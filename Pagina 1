<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<title>Cadastrar Tarefa</title>

<style>
    body {
        background: #eef7ee;
        font-family: Arial, sans-serif;
        display: flex;
        justify-content: center;
        padding-top: 40px;
    }

    .card {
        width: 420px;
        background: white;
        padding: 25px;
        border-radius: 14px;
        box-shadow: 0px 4px 14px rgba(0,0,0,0.15);
        border-left: 8px solid #2e8b57;
    }

    h2 {
        text-align: center;
        color: #2e8b57;
        margin-bottom: 25px;
        font-size: 26px;
    }

    label {
        margin-top: 12px;
        font-weight: bold;
        color: #2e8b57;
    }

    input, select {
        width: 100%;
        padding: 10px;
        margin-top: 5px;
        border-radius: 8px;
        border: 1px solid #bcd9bc;
        outline: none;
        transition: 0.2s;
    }

    input:focus, select:focus {
        border-color: #2e8b57;
        box-shadow: 0 0 4px #2e8b575;
    }

    button {
        margin-top: 22px;
        padding: 12px;
        background: #2e8b57;
        color: white;
        font-size: 16px;
        border: none;
        border-radius: 10px;
        cursor: pointer;
        transition: 0.25s;
        width: 100%;
    }

    button:hover {
        background: #256e45;
    }
</style>
</head>
<body>

<div class="card">
    <h2>Cadastrar Tarefa</h2>

    <label>Prioridade</label>
    <select id="prioridade">
        <option value="">Selecione</option>
        <option value="Baixa">Baixa</option>
        <option value="Normal">Normal</option>
        <option value="Urgente">Urgente</option>
    </select>

    <label>Descrição</label>
    <input id="descricao">

    <label>Local</label>
    <input id="local">

    <label>Recursos Necessários (separe por vírgula)</label>
    <input id="recursos">

    <label>Data Limite</label>
    <input type="datetime-local" id="dataLimite">

    <label>Matrícula</label>
    <input id="matricula" type="number">

    <button onclick="salvar()">Salvar</button>
</div>


<script>
const urlBase = "http://159.65.228.63/tarefas";

async function salvar() {
    const prioridade = document.getElementById("prioridade").value;
    const descricao = document.getElementById("descricao").value;
    const local = document.getElementById("local").value;
    const recursos = document.getElementById("recursos").value.split(",");
    const dataLimite = document.getElementById("dataLimite").value;
    const matricula = document.getElementById("matricula").value;

    // Validação
    if (!prioridade) return alert("Selecione uma prioridade");
    if (!descricao.trim()) return alert("Descrição inválida");
    if (!local.trim()) return alert("Local inválido");
    if (!dataLimite) return alert("Data limite inválida");
    if (!matricula.trim()) return alert("Matrícula inválida");

    const dados = {
        prioridade,
        descricao,
        local,
        recursosNecessarios: recursos.map(r => r.trim()).filter(r => r),
        dataLimite,
        matricula: Number(matricula)
    };

    await fetch(urlBase, {
        method: "POST",
        headers: {"content-type": "application/json"},
        body: JSON.stringify(dados)
    });

    alert("Tarefa cadastrada com sucesso!");
}
</script>

</body>
</html>
